import React, { Component } from 'react';

const PreviewPicture = ({ pictureUrl }) => (
    <img
      className=""
      style={{ height: "300px"}}
      src={pictureUrl}
    />
)

export default PreviewPicture
